export default defineAppConfig({
    ui: {
        strategy: "override",
        primary: "cool",
        avatar: {
            size: {
                xl: "h-32 w-32",
            },
        }
    },
});